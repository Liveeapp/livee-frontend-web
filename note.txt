ssh root@65.21.248.217
root user pass: [REDACTED]

cd /opt/livee-backend

docker-compose -f docker-compose.yaml down

git pull origin master

docker-compose -f docker-compose.yaml build --no-cache

docker-compose -f docker-compose.yaml up -d

# Check all containers are running
docker ps

# View logs
docker-compose -f docker-compose.yaml logs -f






cp nginx.ssl.conf nginx.conf
docker compose restart nginx
# Test health endpoints
curl http://localhost:3001/health  # Authentication
curl http://localhost:3002/health  # Users
curl http://localhost:3003/health  # Email Verification
curl http://localhost:3004/health  # Businesses
curl http://localhost:3005/health  # Content
curl http://localhost:3006/health  # Engagements
docker-compose -f docker-compose.yml logs -f users




docker-compose -f docker-compose.yml logs -f | grep --line-buffered -v "health"



#Web App
ssh root@65.21.248.217
root user pass: [REDACTED]
cd /var/www/liveeapp.io/html

# STOP OLD NGINX TO FREE PORTS 80/443
docker stop livee-nginx
docker rm livee-nginx

docker-compose -f docker-compose.yaml down

git pull origin main

docker-compose -f docker-compose.yaml build --no-cache

docker-compose -f docker-compose.yaml up -d

# View logs
docker-compose -f docker-compose.yaml logs -f




git token: [REDACTED]